# Windows 原生部署的已验证版本（2026-07-11）。
# SAM 3 官方源码仍需单独 clone，并执行：python -m pip install -e D:\MelonDataset\sam3
--extra-index-url https://download.pytorch.org/whl/cu128

torch==2.10.0+cu128
torchvision==0.25.0+cu128
numpy==1.26.4
setuptools==80.9.0

# 官方仓库当前未完整声明、但 Windows 图像推理导入链实际需要的依赖。
einops==0.8.1
triton-windows==3.6.0.post25
pycocotools==2.0.10
psutil==7.2.2

# 本项目的配置、mask 几何与结果导出依赖。
opencv-python-headless==4.11.0.86
PyYAML==6.0.2
pillow==12.2.0

# 官方 SAM 3 直接依赖的当前已验证版本。
timm==1.0.27
tqdm==4.68.4
ftfy==6.1.1
regex==2026.7.10
iopath==0.1.10
typing_extensions==4.15.0
huggingface_hub==1.23.0
